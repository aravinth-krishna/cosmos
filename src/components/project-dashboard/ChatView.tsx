import styles from "./project-dashboard.module.css";

const ChatView = () => {
  return (
    <div className={styles.projectTabView}>
      <h3>Chat</h3>
      <p>Real-time chat interface for project communication will be here.</p>
      {/* Chat interface components */}
    </div>
  );
};

export default ChatView;

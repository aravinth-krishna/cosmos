import styles from "./project-dashboard.module.css";

const DetailsView = () => {
  return (
    <div className={styles.projectTabView}>
      <h3>Details & Settings</h3>
      <p>Project details, settings, and links will be managed here.</p>
      {/* Project details form and settings components */}
    </div>
  );
};

export default DetailsView;
